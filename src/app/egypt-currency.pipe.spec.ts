import { EgyptCurrencyPipe } from './egypt-currency.pipe';

describe('EgyptCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new EgyptCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
